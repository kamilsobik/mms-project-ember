<div class='container-index'>
  <h3>Computerised Maintenance Management System</h3>
  <div class='d-flex m-5'>
    <Shared::Button
      class='btn-main col-5 mx-auto'
      @variant='danger'
      @label='Open request'
      @onClick={{this.onShowRequest}}
    />
    <Shared::Button
      class='btn-main col-5 mx-auto'
      @variant='success'
      @label='KPI'
      @onClick={{this.onShowKpi}}
    />
  </div>
  <div class='d-flex m-5'>
    <Shared::Button
      class='btn-main col-5 mx-auto'
      @variant='primary'
      @label='Archive'
      @onClick={{this.onShowArchive}}
    />
    <Shared::Button
      class='btn-main col-5 mx-auto'
      @variant='warning'
      @label='Logout'
      @onClick={{this.onShowLogout}}
    />
  </div>
</div>