<button
  class='btn'
  type={{this.type}}
  {{on 'click' this.onClick}}
  ...attributes
>
  {{#if @faIcon}} <FaIcon @icon={{@faIcon}} />{{/if}}
  {{#if @src}} <img src={{@src}} alt={{@alt}} />{{/if}}
  {{@label}}</button>